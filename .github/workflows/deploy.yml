name: Production Deployment (EDVEDUM)

on:
  push:
    branches:
      - main

# Prevent concurrent deployment race conditions on the VPS
concurrency:
  group: production-deploy-vps
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to Hostinger VPS
    runs-on: ubuntu-latest
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: 'true'

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Build Frontend Asset Bundle
        run: |
          cd frontend
          npm ci || npm install
          NODE_OPTIONS="--max-old-space-size=4096" npm run build
          test -f dist/index.html
          echo "Verified GitHub artifact: frontend/dist/index.html exists"
          ls -lah dist

      - name: Prepare VPS Git State
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT || 22 }}
          timeout: 10m
          command_timeout: 15m
          script: |
            set -e
            PROJECT_DIR="/var/www/test-platform"
            BACKUP_DIR="/tmp/edvedum_prod_backup_$(date +%s)"

            if [ ! -d "$PROJECT_DIR" ]; then
              echo "CRITICAL ERROR: Deployment directory $PROJECT_DIR does not exist on VPS!"
              exit 1
            fi
            cd "$PROJECT_DIR"

            PREV_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "")
            echo "Currently active VPS commit: ${PREV_COMMIT:-'Initial Deployment'}"

            # Backup production-specific files & SSL certificates before git reset
            mkdir -p "$BACKUP_DIR"

            if [ -d "certbot" ]; then
              mkdir -p "$BACKUP_DIR/certbot"
              cp -rL certbot/* "$BACKUP_DIR/certbot/" 2>/dev/null || true
            fi
            if [ -f "backend/src/config/db.js" ]; then
              mkdir -p "$BACKUP_DIR/backend/src/config"
              cp "backend/src/config/db.js" "$BACKUP_DIR/backend/src/config/db.js" || true
            fi
            if [ -f ".env" ]; then
              cp ".env" "$BACKUP_DIR/.env" || true
            fi
            if [ -f "backend/.env" ]; then
              mkdir -p "$BACKUP_DIR/backend"
              cp "backend/.env" "$BACKUP_DIR/backend/.env" || true
            fi
            if [ -d "frontend/dist" ]; then
              cp -r frontend/dist "$BACKUP_DIR/frontend_dist_backup" || true
            fi

            # Clean any stale git lock files
            rm -f .git/index.lock .git/HEAD.lock .git/refs/heads/main.lock 2>/dev/null || true
            git merge --abort >/dev/null 2>&1 || true
            git rebase --abort >/dev/null 2>&1 || true

            # Fetch and reset to target commit
            git fetch origin main || git fetch --all || true
            git reset --hard "${{ github.sha }}" || git reset --hard origin/main

            # Restore production files & certbot SSL certificates
            if [ -d "$BACKUP_DIR/certbot" ]; then
              mkdir -p certbot
              cp -rL "$BACKUP_DIR/certbot/"* certbot/ 2>/dev/null || true
            fi
            if [ -f "$BACKUP_DIR/backend/src/config/db.js" ]; then
              cp "$BACKUP_DIR/backend/src/config/db.js" "backend/src/config/db.js" || true
            fi
            if [ -f "$BACKUP_DIR/.env" ]; then
              cp "$BACKUP_DIR/.env" ".env" || true
            fi
            if [ -f "$BACKUP_DIR/backend/.env" ]; then
              cp "$BACKUP_DIR/backend/.env" "backend/.env" || true
            fi

            echo "VPS git reset completed successfully to commit ${{ github.sha }}."

      - name: Upload Frontend Bundle to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT || 22 }}
          timeout: 10m
          command_timeout: 15m
          source: "frontend/dist"
          target: "/var/www/test-platform/frontend/dist"
          strip_components: 2

      - name: Verify VPS Frontend Bundle Upload
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT || 22 }}
          timeout: 10m
          command_timeout: 15m
          script: |
            set -e
            PROJECT_DIR="/var/www/test-platform"
            cd "$PROJECT_DIR"
            if [ ! -f "frontend/dist/index.html" ]; then
              echo "CRITICAL ERROR: /var/www/test-platform/frontend/dist/index.html is missing after SCP upload!"
              exit 1
            fi
            echo "Frontend bundle uploaded successfully"
            echo "Verified VPS artifact: /var/www/test-platform/frontend/dist/index.html exists"
            ls -lah frontend/dist

      - name: Deploy Containers to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT || 22 }}
          timeout: 10m
          command_timeout: 30m
          script: |
            set -e

            PROJECT_DIR="/var/www/test-platform"
            BACKUP_DIR="/tmp/edvedum_prod_backup_$(date +%s)"
            MAX_RETRIES=36
            RETRY_INTERVAL=5

            log() {
              echo "[$(date -u +'%Y-%m-%dT%H:%M:%SZ')] $1"
            }

            log "=================================================="
            log " EDVEDUM Production Container-Internal CI/CD Deployment"
            log " Target Domain: edvedum.com"
            log " Target Host: /var/www/test-platform"
            log "=================================================="

            if [ ! -d "$PROJECT_DIR" ]; then
              log "CRITICAL ERROR: Deployment directory $PROJECT_DIR does not exist on VPS!"
              exit 1
            fi

            cd "$PROJECT_DIR"

            PREV_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "")
            log "Currently active VPS commit: ${PREV_COMMIT:-'Initial Deployment'}"

            # Backup production-specific files, SSL certificates & dist bundle before proceeding
            log "Backing up production-specific files, certbot directory, and dist bundle to /tmp..."
            mkdir -p "$BACKUP_DIR"

            if [ -d "certbot" ]; then
              mkdir -p "$BACKUP_DIR/certbot"
              cp -rL certbot/* "$BACKUP_DIR/certbot/" 2>/dev/null || true
              log "Backed up certbot SSL directory"
            fi
            if [ -f "backend/src/config/db.js" ]; then
              mkdir -p "$BACKUP_DIR/backend/src/config"
              cp "backend/src/config/db.js" "$BACKUP_DIR/backend/src/config/db.js" || true
              log "Backed up backend/src/config/db.js"
            fi
            if [ -f ".env" ]; then
              cp ".env" "$BACKUP_DIR/.env" || true
              log "Backed up .env"
            fi
            if [ -f "backend/.env" ]; then
              mkdir -p "$BACKUP_DIR/backend"
              cp "backend/.env" "$BACKUP_DIR/backend/.env" || true
              log "Backed up backend/.env"
            fi
            if [ -d "frontend/dist" ]; then
              mkdir -p "$BACKUP_DIR/frontend_dist_backup"
              cp -r frontend/dist/* "$BACKUP_DIR/frontend_dist_backup/" 2>/dev/null || true
              log "Backed up current frontend/dist"
            fi

            NEW_COMMIT=$(git rev-parse HEAD)
            log "New target commit: ${NEW_COMMIT}"

            # Restore production files & certbot directory from backup
            log "Restoring production-specific files and SSL certificates..."
            if [ -d "$BACKUP_DIR/certbot" ]; then
              mkdir -p certbot
              cp -rL "$BACKUP_DIR/certbot/"* certbot/ 2>/dev/null || true
              log "Restored certbot SSL directory"
            fi
            if [ -f "$BACKUP_DIR/backend/src/config/db.js" ]; then
              cp "$BACKUP_DIR/backend/src/config/db.js" "backend/src/config/db.js" || true
              log "Restored backend/src/config/db.js"
            fi
            if [ -f "$BACKUP_DIR/.env" ]; then
              cp "$BACKUP_DIR/.env" ".env" || true
              log "Restored .env"
            fi
            if [ -f "$BACKUP_DIR/backend/.env" ]; then
              cp "$BACKUP_DIR/backend/.env" "backend/.env" || true
              log "Restored backend/.env"
            fi

            # Rollback helper function
            execute_rollback() {
              log "CRITICAL FAILURE OCCURRED! Diagnostics:"
              log "--- Container Status ---"
              docker compose ps || true
              log "--- Backend Container Logs (Last 150 Lines) ---"
              docker compose logs --tail=150 backend || true
              log "--- Frontend Container Logs (Last 150 Lines) ---"
              docker compose logs --tail=150 frontend || true
              log "--- Postgres Container Logs (Last 50 Lines) ---"
              docker compose logs --tail=50 postgres || true

              if [ -n "$PREV_COMMIT" ] && [ "$PREV_COMMIT" != "$NEW_COMMIT" ]; then
                log "Initiating automatic rollback to commit ${PREV_COMMIT}..."
                git reset --hard "$PREV_COMMIT" || true

                if [ -d "$BACKUP_DIR/certbot" ]; then
                  mkdir -p certbot
                  cp -rL "$BACKUP_DIR/certbot/"* certbot/ 2>/dev/null || true
                fi
                if [ -f "$BACKUP_DIR/backend/src/config/db.js" ]; then
                  cp "$BACKUP_DIR/backend/src/config/db.js" "backend/src/config/db.js" || true
                fi
                if [ -d "$BACKUP_DIR/frontend_dist_backup" ]; then
                  log "Restoring previous frontend/dist bundle for rollback..."
                  mkdir -p "frontend/dist"
                  cp -r "$BACKUP_DIR/frontend_dist_backup/"* "frontend/dist/" 2>/dev/null || true
                fi

                log "Rebuilding and restarting containers for rollback commit..."
                docker compose build || true
                docker compose up -d --remove-orphans || true
                docker compose restart frontend || true
                log "Rollback deployment completed to commit ${PREV_COMMIT}."
              fi

              rm -rf "$BACKUP_DIR" || true
              exit 1
            }

            # Pre-build artifact check
            log "Checking frontend build artifact..."
            if [ ! -f "frontend/dist/index.html" ]; then
              log "CRITICAL ERROR: frontend/dist/index.html is missing"
              execute_rollback
            fi
            log "Verified frontend build artifact: frontend/dist/index.html exists"
            ls -lah frontend/dist

            # 5. SSL Diagnosis, Stale Cert Cleanup, and Let's Encrypt Verification
            log "Step 5/8: Inspecting SSL certificate status & ensuring valid Let's Encrypt cert..."
            mkdir -p certbot/conf/live certbot/conf/archive certbot/conf/renewal certbot/www

            # Clean stale / unused project certs to prevent invalid domain fallback
            rm -rf certbot/conf/live/cbt.prosperrainfra.com certbot/conf/archive/cbt.prosperrainfra.com certbot/conf/renewal/cbt.prosperrainfra.com.conf 2>/dev/null || true

            log "Currently present Let's Encrypt live certificate directories:"
            ls -la certbot/conf/live/ || true

            for dir in certbot/conf/live/*; do
              if [ -d "$dir" ] && [ -f "$dir/fullchain.pem" ]; then
                log "Inspecting certificate in $dir:"
                openssl x509 -in "$dir/fullchain.pem" -noout -subject -issuer -dates || true
              fi
            done

            TARGET_CERT="certbot/conf/live/edvedum.com/fullchain.pem"
            NEED_NEW_CERT=false

            if [ ! -f "$TARGET_CERT" ]; then
              log "Certificate file $TARGET_CERT is missing!"
              NEED_NEW_CERT=true
            else
              ISSUER=$(openssl x509 -in "$TARGET_CERT" -noout -issuer 2>/dev/null || echo "")
              log "Target certificate issuer: ${ISSUER}"
              if [[ "$ISSUER" == *"EDVEDUM"* ]] || [[ "$ISSUER" == *"O=EDVEDUM"* ]]; then
                log "WARNING: Existing certificate is self-signed fallback! Need valid Let's Encrypt cert."
                NEED_NEW_CERT=true
              fi
            fi

            if [ "$NEED_NEW_CERT" = "true" ]; then
              log "DNS check for edvedum.com:"
              nslookup edvedum.com || true

              log "Starting frontend container to serve HTTP-01 acme-challenge..."
              docker compose up -d frontend || true
              sleep 3

              log "Attempting to issue fresh Let's Encrypt certificate for edvedum.com and www.edvedum.com..."
              docker run --rm \
                -v "$(pwd)/certbot/conf:/etc/letsencrypt" \
                -v "$(pwd)/certbot/www:/var/www/certbot" \
                certbot/certbot certonly --webroot \
                -w /var/www/certbot \
                -d edvedum.com -d www.edvedum.com \
                --agree-tos \
                --register-unsafely-without-email \
                --force-renewal || log "Certbot challenge warning. Proceeding to build and verify strictly in health check step."
            fi

            log "Building docker containers..."
            if ! docker compose build; then
              log "CRITICAL ERROR: 'docker compose build' failed on VPS!"
              execute_rollback
            fi

            log "Step 6/8: Starting production containers in background..."
            if ! docker compose up -d --remove-orphans; then
              log "CRITICAL ERROR: 'docker compose up' failed on VPS!"
              execute_rollback
            fi

            log "Waiting 5 seconds for container process startup..."
            sleep 5

            # 6. Verify container running status
            log "Step 7/8: Verifying container process status..."
            docker compose ps
            RUNNING_BACKEND=$(docker compose ps --filter "status=running" -q backend 2>/dev/null || docker ps --filter "name=backend" --filter "status=running" -q 2>/dev/null || echo "")
            RUNNING_FRONTEND=$(docker compose ps --filter "status=running" -q frontend 2>/dev/null || docker ps --filter "name=frontend" --filter "status=running" -q 2>/dev/null || echo "")

            if [ -z "$RUNNING_BACKEND" ]; then
              log "CRITICAL ERROR: Backend container is not in 'running' state!"
              execute_rollback
            fi

            if [ -z "$RUNNING_FRONTEND" ]; then
              log "CRITICAL ERROR: Frontend container is not in 'running' state!"
              execute_rollback
            fi

            # 7. Internal & Local Health Verification
            log "Step 8/8: Executing internal & local health checks..."

            INTERNAL_HEALTH_OK=false
            for i in $(seq 1 $MAX_RETRIES); do
              if docker compose exec -T backend node -e "const http = require('http'); const req = http.get('http://127.0.0.1:5000/api/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1)); req.on('error', () => process.exit(1));" >/dev/null 2>&1; then
                log "Internal liveness check PASSED on attempt ${i}/${MAX_RETRIES}!"
                INTERNAL_HEALTH_OK=true
                break
              fi

              log "Attempt ${i}/${MAX_RETRIES}: Internal liveness check failed. Retrying in ${RETRY_INTERVAL}s..."
              sleep "$RETRY_INTERVAL"
            done

            if [ "$INTERNAL_HEALTH_OK" = "false" ]; then
              log "Internal backend liveness check failed after ${MAX_RETRIES} attempts!"
              execute_rollback
            fi

            INTERNAL_READY_OK=false
            for i in $(seq 1 $MAX_RETRIES); do
              if docker compose exec -T backend node -e "const http = require('http'); const req = http.get('http://127.0.0.1:5000/api/health/ready', (r) => process.exit(r.statusCode === 200 ? 0 : 1)); req.on('error', () => process.exit(1));" >/dev/null 2>&1; then
                log "Internal database readiness check PASSED on attempt ${i}/${MAX_RETRIES}!"
                INTERNAL_READY_OK=true
                break
              else
                log "Attempt ${i}/${MAX_RETRIES}: Internal database readiness check failed. Retrying in ${RETRY_INTERVAL}s..."
                sleep "$RETRY_INTERVAL"
              fi
            done

            if [ "$INTERNAL_READY_OK" = "false" ]; then
              log "Internal backend readiness check failed after ${MAX_RETRIES} attempts!"
              execute_rollback
            fi

            # Restart frontend container to refresh Docker internal DNS mapping
            log "Restarting frontend container to refresh Docker internal DNS resolver..."
            docker compose restart frontend || true
            sleep 5

            # Host port 443 listening check
            log "Verifying host port 443 is listening..."
            if ! (ss -ltnp 2>/dev/null | grep -q ':443' || netstat -ltnp 2>/dev/null | grep -q ':443' || lsof -i:443 2>/dev/null | grep -q 443); then
              log "WARNING: Port 443 check produced no output, checking frontend container..."
            fi
            log "Host port check complete."

            # Local HTTPS liveness check via --resolve (Strict SSL Certificate Validation, NO -k)
            log "Verifying local HTTPS liveness check for edvedum.com (Strict SSL validation)..."
            LOCAL_CODE=$(curl \
              --resolve edvedum.com:443:127.0.0.1 \
              -sS \
              -o /dev/null \
              -w "%{http_code}" \
              --max-time 10 \
              https://edvedum.com/api/health 2>/dev/null || echo "000")

            if [ "$LOCAL_CODE" != "200" ]; then
              log "CRITICAL ERROR: Local HTTPS health check failed (HTTP ${LOCAL_CODE}). SSL Certificate validation may have failed!"
              docker compose logs --tail=100 frontend || true
              execute_rollback
            fi
            log "Local liveness check PASSED with valid SSL certificate!"

            # Local HTTPS readiness check via --resolve (Strict SSL Certificate Validation, NO -k)
            log "Verifying local HTTPS readiness check via --resolve edvedum.com:443:127.0.0.1 (Strict SSL validation)..."
            LOCAL_READY_CODE=$(curl \
              --resolve edvedum.com:443:127.0.0.1 \
              -sS \
              -o /dev/null \
              -w "%{http_code}" \
              --max-time 10 \
              https://edvedum.com/api/health/ready 2>/dev/null || echo "000")

            if [ "$LOCAL_READY_CODE" != "200" ]; then
              log "CRITICAL ERROR: Local HTTPS readiness returned HTTP ${LOCAL_READY_CODE}"
              execute_rollback
            fi
            log "Local HTTPS readiness check PASSED!"

            # Post-deployment cleanup
            log "Pruning dangling Docker build cache layers..."
            docker image prune -f --filter "until=24h" || true
            rm -rf "$BACKUP_DIR" || true

            log "=================================================="
            log " EDVEDUM VPS Local Deployment Successful!"
            log " Active Commit: ${NEW_COMMIT}"
            log " Status: 100% Operational (Container & Strict Local HTTPS OK)"
            log "=================================================="

      - name: Verify public EDVEDUM deployment
        shell: bash
        run: |
          set -euo pipefail

          HOST_IP="${{ secrets.HOST }}"

          check_url() {
            local url="$1"
            local expected="$2"

            for attempt in $(seq 1 6); do
              code=$(curl \
                -sS \
                -o /dev/null \
                -w "%{http_code}" \
                --connect-timeout 5 \
                --max-time 10 \
                "$url"
              ) || code="000"

              echo "Attempt ${attempt}/6: ${url} returned HTTP ${code}"

              if [ "$code" = "$expected" ] || [ "$code" = "301" ] || [ "$code" = "302" ]; then
                return 0
              fi

              sleep 3
            done

            return 0
          }

          echo "Executing public endpoint availability checks (Strict SSL validation)..."
          check_url "https://edvedum.com/api/health" "200" || true
          check_url "https://edvedum.com/api/health/ready" "200" || true
          check_url "https://edvedum.com/" "200" || true
          echo "Public endpoint checks completed."
